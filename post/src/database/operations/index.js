module.exports = {
    PostQuery: require('./data-query'),
    PostManipulation: require('./data-manipulation'),
}
//Todo: change folder name from "queries" to "operations". [operations]: data query, data minipulation (insert, delete, update)